services:
  app:
    build:
      context: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app
    environment:
      APP_PORT: 8000
    env_file:
      - ./server/.env.development
    entrypoint: /bin/bash -c "poetry run uvicorn perfi:app --host 0.0.0.0 --port 8000"
